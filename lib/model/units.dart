import 'modules.dart';

class Units {
  final String title;
  final int modules;
  final List<Modules> modulesList;

  Units(
      {required this.title, required this.modules, required this.modulesList});
}
